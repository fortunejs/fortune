module.exports = fetch;


/**
 * Retrieve entities from database.
 *
 * @param {Object} adapter
 * @param {String} resource
 * @param {Array} ids array of strings
 * @return {Array}
 */
function fetch (adapter, resource, ids) {
	var array = [];

	ids.forEach(function (id) {

	});

	return array;
}
