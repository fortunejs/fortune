<p class="intro">Fortune.js is a library for managing structured data in Node.js and web browsers. It consists of a data abstraction layer and networking functions.</p>
<p>The latest version is <strong>{{version}}</strong>, get it from <code>npm</code>, or <a href="/assets/fortune.min.js">download the browser version</a>:</p>
<pre class="install"><code>npm install <span class="hljs-literal">fortune</span> <span class="hljs-string">--save</span></code></pre>
