<div class="home">
  <h4>Info</h4>
  <p>Fortune is an <em>I/O library for web applications</em>. The main features it offers are record types with first-class support for relationships, and workflows for persistence and serialization. A few defaults are included to be useful out of the box, including an <a href="https://github.com/fortunejs/fortune/blob/master/lib/adapter/adapters/nedb/index.js">embedded database adapter</a>, and <a href="https://github.com/fortunejs/fortune/tree/master/lib/serializer/serializers">serializers for standard media types</a>.</p>
  <p>The latest version is <strong>{{version}}</strong>. Get it from <code>npm</code>:</p>
  <div class="download">
    <pre><code>npm install fortune --save</code></pre><!--
    --><a href="https://github.com/fortunejs/fortune/archive/master.zip"><span>⚑</span>Download</a>
  </div>
  <hr>
  <h4>Example</h4>
</div>
